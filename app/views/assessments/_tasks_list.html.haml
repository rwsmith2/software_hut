-# 
  Partial which lists all the tasks in the system

.list-group.overflow-auto.p-1
  //If statement checks if there are any tasks to display
  - if tasks != nil
    //Loops through each task, creating a link which will fetch the selected task when clicked
    - tasks.each do |tasks|
      -@task = Task.find_by(task_id: tasks.task_id)
      = link_to @task.task_title, fetch_task_path(task_id: tasks.task_id), remote: true, class: "list-group-item list-group-item-action", type: "button" 
  - else
    No tasks available
    