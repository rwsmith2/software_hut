-# 
  Javascript file used to replace and render an updated tasks list, after successfully searching tasks
$('#task-list-panel').html("#{escape_javascript(render partial: 'tasks_list', locals: { tasks: @tasks } )}  "); 